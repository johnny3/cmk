<h1>NewsletterArticle list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Createdat</th>
            <th>Updatedat</th>
            <th>Slug</th>
            <th>Title</th>
            <th>Picture</th>
            <th>Body</th>
            <th>Reservationlink</th>
            <th>Position</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('newsletter_article_admin_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{% if entity.createdAt %}{{ entity.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
            <td>{% if entity.updatedAt %}{{ entity.updatedAt|date('Y-m-d H:i:s') }}{% endif %}</td>
            <td>{{ entity.slug }}</td>
            <td>{{ entity.title }}</td>
            <td>{{ entity.picture }}</td>
            <td>{{ entity.body }}</td>
            <td>{{ entity.reservationLink }}</td>
            <td>{{ entity.position }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('newsletter_article_admin_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('newsletter_article_admin_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('newsletter_article_admin_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
