{% extends "::baseAdmin.html.twig" %}

{% block body_admin %}

<h1>Subscription list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Titre</th>
            <th>Sous titre</th>
            <th>Tarif plein</th>
            <th>Tarif réduit</th>
            <th>Tarif PF</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td>{{ entity.title }}</td>
            <td>{{ entity.subtitle }}</td>
            <td>{{ entity.maxPrice }}</td>
            <td>{{ entity.mediumPrice }}</td>
            <td>{{ entity.lowPrice }}</td>
            <td>
                <a href="{{ path('subscription_admin_show', { 'id': entity.id }) }}" title="Show"><img src="{{ asset('bundles/shakyamouniadmin/images/icons/eye-icon.png') }}" alt="Show" /></a><br/>
                <a href="{{ path('subscription_admin_edit', { 'id': entity.id }) }}" title="Edit"><img src="{{ asset('bundles/shakyamouniadmin/images/icons/pencil.png') }}" alt="Edit" /></a><br/>
                {#<a href="{{ path('subscription_admin_delete', { 'id': entity.id }) }}" title="Delete"><img src="{{ asset('bundles/shakyamouniadmin/images/icons/cross.png') }}" alt="Delete" /></a>#} 
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>
{% endblock %} 