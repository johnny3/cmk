{% extends "::baseAdmin.html.twig" %}

{% block body_admin %}

<h1>SlideShow creation</h1>

{% for flashMessage in app.session.flashbag.get('notice') %}
    <div class="flash-notice">
        {{ flashMessage }}
    </div>
{% endfor %}

<form action="{{ path('slideshow_admin_create') }}" method="post" {{ form_enctype(form) }}>
    <div id="shakyamouni_sitebundle_slideshowtype">
        <p>
            {{ form_label(form.title, 'Titre') }}
            {{ form_widget(form.title, { 'attr': {'class': 'text-input medium-input'} })  }} {% if form_errors(form.title) %}<span class="input-notification error png_bg">{{ form_errors(form.title) }}</span> {% endif %}
        </p>
    </div>
        
    {{ form_rest(form) }}
        
    <p>
        <button class="button" type="submit">Créer</button>
    </p>
</form>
{% endblock %}