{% extends "::baseAdmin.html.twig" %}

{% block body_admin %}
<h1>Slider list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Titre</th>
            <th>Sous-titre</th>
            <th>Image</th>
            <th>Position</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
            <tr>
                <td>{{ entity.title }}</td>
                <td>{{ entity.subtitle }}</td>
                <td>{{ entity.picture }}</td>
                <td>{{ entity.position }}</td>
                <td>
                    <a href="{{ path('slider_admin_edit', { 'id': entity.id }) }}" title="Edit"><img src="{{ asset('bundles/shakyamouniadmin/images/icons/pencil.png') }}" alt="Edit" /></a>
                    <a href="{{ path('slider_admin_delete', { 'id': entity.id }) }}" title="Delete" class="action-delete"><img src="{{ asset('bundles/shakyamouniadmin/images/icons/cross.png') }}" alt="Delete" /></a> 
                </td>
            </tr>
    {% endfor %}
        </tbody>
    </table>
{% endblock %} 